LandclaimObjectConfig = 
{
    min_height = 0,
    max_height = 6000,
    decay_per_interval = 1, -- 1 health decay per decay interval (1hr) to objects on expired claims
    min_object_decay_health = 200
}